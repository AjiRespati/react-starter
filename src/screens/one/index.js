
import { connect } from 'react-redux'
import OneScreen from './OneScreen'

const mapStateToProps = state => {
  return {

  }
}

const mapDispatchToProps = dispatch => {
  return {

  }
}

const One = connect(mapStateToProps, mapDispatchToProps)(OneScreen)

export default One